import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#1B2E49'
    },
    secondary: {
      main: '#F3F7FA'
    }
  }
})
export default theme
